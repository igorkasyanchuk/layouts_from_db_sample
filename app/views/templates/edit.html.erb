<h1>Editing template</h1>

<% form_for(@template) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :layout %><br />
    <%= f.text_area :layout %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @template %> |
<%= link_to 'Back', templates_path %>